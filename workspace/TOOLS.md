# Tool Constraints

- Shell timeout: 60 seconds — use background processes for long tasks
- Shell output limit: 10,000 characters
- Use `web_search` for property listings, area info, infrastructure plans
- Use `kb` skill for persistent storage and retrieval
- Use `agent-comm` to consult other agents (Ada for financial analysis, etc.)
